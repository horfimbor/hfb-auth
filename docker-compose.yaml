
services:

  mariadb:
    image: mariadb:10.7
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: password
    ports:
      - "3306:3306"

  phpmyadmin:
    image: phpmyadmin
    restart: always
    ports:
      - "3000:80"
    environment:
      - PMA_ARBITRARY=1