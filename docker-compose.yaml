version: "3.4"

services:


  mariadb:
    image: mariadb:10.7
    environment:
      MARIADB_ROOT_PASSWORD: password
    ports:
      - "3306:3306"
    volumes:
      - type: volume
        source: mariadb-volume-data
        target: /var/lib/mysql